[project]
name = "plot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.0",
    "numpy>=2.5.0",
    "pandas[parquet]>=3.0.3",
    "scipy>=1.18.1",
    "tabulate>=0.10.0",
]

[dependency-groups]
dev = [
    "ipython>=9.17.1",
]
